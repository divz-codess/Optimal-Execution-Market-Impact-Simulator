# Almgren-Chriss Execution Analytics — Python dependencies
#
# Install: pip install -r python/requirements.txt
# Run dashboard: streamlit run python/dashboard/app.py

# ── Core data stack ───────────────────────────────────────────────────────────
numpy>=1.26
pandas>=2.2
scipy>=1.13

# ── Market data ───────────────────────────────────────────────────────────────
yfinance>=0.2.40

# ── Dashboard & visualisation ─────────────────────────────────────────────────
streamlit>=1.35
plotly>=5.22

# ── Build / dev tools (optional, not required for Streamlit Cloud) ────────────
# cmake   — install via system package manager or conda
